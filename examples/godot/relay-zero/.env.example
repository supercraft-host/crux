CRUX_URL=https://crux.supercraft.host
CRUX_PROJECT_ID=
CRUX_ENVIRONMENT_ID=
CRUX_SERVER_TOKEN=
PUBLIC_ADDRESS=127.0.0.1
REGION=eu-west
PORT=9080

# Production allocators inject one match ID and token into each match process.
MATCH_ID=
EXPECTED_JOIN_TOKEN=

# Local development only. This bypasses join-token matching, not player JWT verification.
RELAY_ALLOW_INSECURE_DEV_AUTH=1

LEADERBOARD_WEEKLY_ID=
LEADERBOARD_FASTEST_ID=
